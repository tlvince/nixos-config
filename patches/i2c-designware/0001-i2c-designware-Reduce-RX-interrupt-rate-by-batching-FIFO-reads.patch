From ad9d08a72850eb662ae4f7a85b5060ca397f8c75 Mon Sep 17 00:00:00 2001
From: Your Name <your.email@example.com>
Date: Thu, 3 Sep 2026 12:11:33 +0100
Subject: [PATCH 1/2] i2c: designware: Reduce RX interrupt rate by batching
 FIFO reads

The DesignWare I2C controller in master mode configures the RX FIFO
threshold (IC_RX_TL) to 0, so the controller raises an RX_FULL
interrupt as soon as any data is in the RX FIFO.  For I2C devices
that stream data in small chunks, such as I2C-HID touchpads, this
causes a large interrupt amplification: the I2C controller can
generate 1000-3000 interrupts per second while the peripheral
itself only signals ~140 events per second.

Fix this by:

1.  Setting IC_RX_TL to rx_fifo_depth / 2 so the controller only
    raises RX_FULL when the FIFO is at least half full, batching
    multiple bytes per interrupt.

2.  Enabling the RX_DONE interrupt in the master mask so that the
    last partial batch of a read (bytes that do not fill the FIFO
    past the threshold) can be drained.

3.  Draining the RX FIFO in the ISR on RX_DONE and STOP_DET, and
    explicitly clearing a latched STOP_DET after the drain.

Measured on an AMD Ryzen AI 5 340 (Krackan Point) with an ELAN
I2C-HID touchpad connected via AMDI0010: the I2C controller IRQ
rate dropped from ~4890 to ~266 interrupts per second during
touchpad movement.

Link: https://bugzilla.kernel.org/show_bug.cgi?id=218169
Signed-off-by: Your Name <your.email@example.com>
---
 drivers/i2c/busses/i2c-designware-common.c |  2 +-
 drivers/i2c/busses/i2c-designware-core.h   |  3 ++-
 drivers/i2c/busses/i2c-designware-master.c | 21 +++++++++++++++++++++
 3 files changed, 24 insertions(+), 2 deletions(-)

diff --git a/drivers/i2c/busses/i2c-designware-common.c b/drivers/i2c/busses/i2c-designware-common.c
index e4dfa2ec5..f9343bc7e 100644
--- a/drivers/i2c/busses/i2c-designware-common.c
+++ b/drivers/i2c/busses/i2c-designware-common.c
@@ -364,7 +364,7 @@ static void i2c_dw_configure_mode(struct dw_i2c_dev *dev, int mode)
 	switch (mode) {
 	case DW_IC_MASTER:
 		regmap_write(dev->map, DW_IC_TX_TL, dev->tx_fifo_depth / 2);
-		regmap_write(dev->map, DW_IC_RX_TL, 0);
+		regmap_write(dev->map, DW_IC_RX_TL, dev->rx_fifo_depth / 2);
 		regmap_write(dev->map, DW_IC_CON, dev->master_cfg);
 		break;
 	case DW_IC_SLAVE:
diff --git a/drivers/i2c/busses/i2c-designware-core.h b/drivers/i2c/busses/i2c-designware-core.h
index c71aa2dd3..e10b029fb 100644
--- a/drivers/i2c/busses/i2c-designware-core.h
+++ b/drivers/i2c/busses/i2c-designware-core.h
@@ -118,7 +118,8 @@
 						 DW_IC_INTR_TX_ABRT | \
 						 DW_IC_INTR_STOP_DET)
 #define DW_IC_INTR_MASTER_MASK			(DW_IC_INTR_DEFAULT_MASK | \
-						 DW_IC_INTR_TX_EMPTY)
+						 DW_IC_INTR_TX_EMPTY | \
+						 DW_IC_INTR_RX_DONE)
 #define DW_IC_INTR_SLAVE_MASK			(DW_IC_INTR_DEFAULT_MASK | \
 						 DW_IC_INTR_RX_UNDER | \
 						 DW_IC_INTR_RD_REQ)
diff --git a/drivers/i2c/busses/i2c-designware-master.c b/drivers/i2c/busses/i2c-designware-master.c
index 7a301c8b6..87453ff7b 100644
--- a/drivers/i2c/busses/i2c-designware-master.c
+++ b/drivers/i2c/busses/i2c-designware-master.c
@@ -633,6 +633,8 @@ static u32 i2c_dw_read_clear_intrbits(struct dw_i2c_dev *dev)
 
 static void i2c_dw_process_transfer(struct dw_i2c_dev *dev, unsigned int stat)
 {
+	unsigned int dummy;
+
 	if (stat & DW_IC_INTR_TX_ABRT) {
 		dev->cmd_err |= DW_IC_ERR_TX_ABRT;
 		dev->status &= ~STATUS_MASK;
@@ -652,6 +654,25 @@ static void i2c_dw_process_transfer(struct dw_i2c_dev *dev, unsigned int stat)
 	if (stat & DW_IC_INTR_TX_EMPTY)
 		i2c_dw_xfer_msg(dev);
 
+	/*
+	 * With a non-zero RX_TL, the last partial batch of a read may
+	 * not have triggered RX_FULL.  RX_DONE signals that the last
+	 * byte of a read operation has been received; STOP_DET signals
+	 * the end of the transfer.  In both cases drain the RX FIFO.
+	 */
+	if (stat & (DW_IC_INTR_RX_DONE | DW_IC_INTR_STOP_DET)) {
+		i2c_dw_read(dev);
+
+		/*
+		 * read_clear_intrbits() defers clearing STOP_DET while
+		 * rx_outstanding > 0 to avoid completing before all data
+		 * has been read.  After the drain above, explicitly clear
+		 * a latched STOP_DET to avoid a spurious re-interrupt.
+		 */
+		if ((stat & DW_IC_INTR_STOP_DET) && dev->rx_outstanding == 0)
+			regmap_read(dev->map, DW_IC_CLR_STOP_DET, &dummy);
+	}
+
 	/* Abort if we detect a STOP in the middle of a read or a write */
 	if ((stat & DW_IC_INTR_STOP_DET) &&
 	    (dev->status & (STATUS_READ_IN_PROGRESS | STATUS_WRITE_IN_PROGRESS))) {
-- 
2.55.0

